# Contributing to Nothing Hustle

## Setup
```bash
npm install && cp .env.example .env.local && npm run dev
```

## Commit Convention
```
feat(agent): add freelance writing sector agent
feat(gigs): add template library
fix: resolve earnings calculation rounding error
test: add gig execution unit tests
```
